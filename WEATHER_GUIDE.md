# 🌤️ Руководство по виджету погоды

## Обзор

Виджет погоды отображает актуальную температуру и погодные условия в Бангкоке (или другом городе). Использует OpenWeatherMap API для получения данных.

## 🎯 Возможности

- ✅ Текущая температура в реальном времени
- ✅ Описание погодных условий
- ✅ Иконка погоды
- ✅ Ощущаемая температура
- ✅ Влажность воздуха
- ✅ Автоматическое обновление каждые 30 минут
- ✅ Красивый градиентный дизайн
- ✅ Состояния загрузки и ошибки
- ✅ Фиксированное положение в углу

## 🔧 Настройка

### Шаг 1: Получение API ключа

1. Зарегистрируйтесь на [OpenWeatherMap](https://openweathermap.org/)
2. Перейдите в [API Keys](https://home.openweathermap.org/api_keys)
3. Создайте новый API ключ (бесплатный план включает 1000 запросов/день)
4. Скопируйте ключ

### Шаг 2: Настройка проекта

Создайте или обновите файл `.env.local` в корне проекта:

```env
# OpenWeatherMap API Key
NEXT_PUBLIC_OPENWEATHER_API_KEY=ваш_ключ_здесь
```

**Важно**: Ключ должен начинаться с `NEXT_PUBLIC_` чтобы быть доступным на клиенте.

### Шаг 3: Перезапуск сервера

```bash
npm run dev
```

Виджет начнет работать автоматически!

## 📦 Структура

### Архитектура FSD

```
src/
├── shared/
│   ├── api/
│   │   └── weather.ts           # API для погоды
│   └── hooks/
│       └── use-weather.ts       # Хук для получения данных
└── widgets/
    └── weather-widget/
        ├── ui/
        │   └── weather-widget.tsx  # Компонент виджета
        └── index.ts
```

### Компоненты

1. **`weather.ts`** - API функции и типы
2. **`use-weather.ts`** - React хук для работы с погодой
3. **`weather-widget.tsx`** - UI компонент виджета

## 🎨 Использование

### Базовое использование

```tsx
import { WeatherWidget } from '@/widgets/weather-widget';

<WeatherWidget />
```

### С кастомными классами

```tsx
<WeatherWidget className="shadow-2xl" />
```

### Позиционирование

```tsx
{/* Правый верхний угол */}
<div className="fixed top-24 right-8 z-40 w-72">
  <WeatherWidget />
</div>

{/* Левый верхний угол */}
<div className="fixed top-24 left-8 z-40 w-72">
  <WeatherWidget />
</div>

{/* В потоке контента */}
<div className="w-full max-w-md">
  <WeatherWidget />
</div>
```

## 🔌 API Функции

### `fetchBangkokWeather()`

Получить погоду в Бангкоке:

```typescript
import { fetchBangkokWeather } from '@/shared/api';

const weather = await fetchBangkokWeather();
if (weather) {
  console.log(`${weather.temp}°C в ${weather.city}`);
}
```

### `fetchWeatherByCoords(lat, lon)`

Получить погоду для любого места:

```typescript
import { fetchWeatherByCoords } from '@/shared/api';

// Погода в Пхукете
const weather = await fetchWeatherByCoords(7.880758, 98.392179);
```

### `useWeather(lat, lon)`

React хук для погоды:

```typescript
import { useWeather } from '@/shared/hooks';

function MyComponent() {
  const { weather, loading, error } = useWeather(13.736717, 100.523186);
  
  if (loading) return <div>Загрузка...</div>;
  if (error) return <div>Ошибка: {error}</div>;
  
  return <div>{weather?.temp}°C</div>;
}
```

## 🎨 Кастомизация дизайна

### Изменение цветов

Отредактируйте `weather-widget.tsx`:

```tsx
{/* Зеленый градиент */}
<div className="bg-gradient-to-br from-green-500 to-green-600 ...">

{/* Оранжевый градиент */}
<div className="bg-gradient-to-br from-orange-500 to-orange-600 ...">

{/* Фиолетовый градиент */}
<div className="bg-gradient-to-br from-purple-500 to-purple-600 ...">

{/* Темная тема */}
<div className="bg-gradient-to-br from-gray-800 to-gray-900 ...">
```

### Изменение размера

```tsx
{/* Маленький виджет */}
<div className="w-64">
  <WeatherWidget />
</div>

{/* Средний (по умолчанию) */}
<div className="w-72">
  <WeatherWidget />
</div>

{/* Большой */}
<div className="w-96">
  <WeatherWidget />
</div>
```

### Убрать дополнительную информацию

Удалите блок в `weather-widget.tsx`:

```tsx
{/* Этот блок можно удалить */}
<div className="mt-4 pt-4 border-t border-white/20 ...">
  ...
</div>
```

## 🌍 Добавление других городов

### Вариант 1: Новый виджет для другого города

```tsx
// src/widgets/weather-widget/ui/phuket-weather.tsx
export const PhuketWeather = () => {
  const { weather, loading, error } = useWeather(7.880758, 98.392179);
  // ... остальной код
};
```

### Вариант 2: Параметризованный виджет

```tsx
interface WeatherWidgetProps {
  lat: number;
  lon: number;
  cityName?: string;
}

export const WeatherWidget = ({ lat, lon, cityName }: WeatherWidgetProps) => {
  const { weather, loading, error } = useWeather(lat, lon);
  // ...
};

// Использование
<WeatherWidget 
  lat={7.880758} 
  lon={98.392179} 
  cityName="Пхукет" 
/>
```

### Популярные координаты

```typescript
// Бангкок
{ lat: 13.736717, lon: 100.523186 }

// Пхукет
{ lat: 7.880758, lon: 98.392179 }

// Чианг Май
{ lat: 18.788135, lon: 98.985520 }

// Паттайя
{ lat: 12.927608, lon: 100.877083 }

// Краби
{ lat: 8.086447, lon: 98.906311 }
```

## 📊 Типы данных

### WeatherData

```typescript
interface WeatherData {
  temp: number;          // Температура (°C)
  feels_like: number;    // Ощущается как (°C)
  humidity: number;      // Влажность (%)
  description: string;   // Описание погоды
  icon: string;         // Код иконки
  city: string;         // Название города
}
```

## ⚙️ Конфигурация

### Изменение интервала обновления

В `use-weather.ts`:

```typescript
// По умолчанию: 30 минут
const interval = setInterval(loadWeather, 30 * 60 * 1000);

// Каждые 15 минут
const interval = setInterval(loadWeather, 15 * 60 * 1000);

// Каждый час
const interval = setInterval(loadWeather, 60 * 60 * 1000);
```

### Изменение единиц измерения

В `src/shared/api/weather.ts`:

```typescript
export const WEATHER_CONFIG = {
  // ...
  units: 'metric',     // Цельсий (по умолчанию)
  // units: 'imperial', // Фаренгейт
  // units: 'standard', // Кельвин
} as const;
```

### Изменение языка

```typescript
export const WEATHER_CONFIG = {
  // ...
  lang: 'ru',    // Русский (по умолчанию)
  // lang: 'en', // Английский
  // lang: 'th', // Тайский
} as const;
```

## 🎯 Примеры состояний

### Загрузка

```tsx
// Показывается автоматически при загрузке
// Скелетон с анимацией pulse
```

### Успех

```tsx
// Красивая карточка с градиентом
// Температура, иконка, описание
```

### Ошибка

```tsx
// Простая карточка с иконкой предупреждения
// Сообщение "Данные недоступны"
```

## 🐛 Troubleshooting

### Данные не загружаются

1. ✅ Проверьте API ключ в `.env.local`
2. ✅ Убедитесь что файл называется `.env.local` (не `.env`)
3. ✅ Перезапустите dev-сервер после добавления ключа
4. ✅ Проверьте консоль браузера на ошибки

### API ключ не работает

1. ✅ Подождите 10-15 минут после создания ключа
2. ✅ Проверьте что ключ активен в [личном кабинете](https://home.openweathermap.org/api_keys)
3. ✅ Проверьте лимиты (1000 запросов/день на бесплатном плане)

### Показывает неправильный город

Проверьте координаты:
```typescript
// Должны быть в формате: широта, долгота
useWeather(13.736717, 100.523186) // ✅ Правильно
useWeather(100.523186, 13.736717) // ❌ Неправильно (перепутаны)
```

### Иконки не загружаются

Иконки загружаются с сервера OpenWeatherMap:
- URL: `https://openweathermap.org/img/wn/{icon}@2x.png`
- Используется `unoptimized` для Next.js Image
- Проверьте подключение к интернету

## 💡 Расширенные возможности

### Добавление прогноза на несколько дней

```typescript
// В weather.ts добавить:
export async function fetchWeatherForecast(lat: number, lon: number) {
  const url = `${baseUrl}/forecast?lat=${lat}&lon=${lon}&appid=${apiKey}&lang=${lang}&units=${units}`;
  // ... обработка
}
```

### Добавление восхода/заката

API уже предоставляет эти данные в response:
```typescript
interface WeatherResponse {
  // ... existing fields
  sys: {
    sunrise: number;  // Unix timestamp
    sunset: number;   // Unix timestamp
  };
}
```

### Анимированные иконки

Можно использовать сторонние библиотеки:
- [React Weather Icons](https://github.com/Maisharu/react-open-weather)
- [Animated Weather Icons](https://github.com/basmilius/weather-icons)

## 📚 Полезные ссылки

- [OpenWeatherMap API Documentation](https://openweathermap.org/api)
- [Current Weather API](https://openweathermap.org/current)
- [Weather Icons](https://openweathermap.org/weather-conditions)
- [API FAQ](https://openweathermap.org/faq)

## 🎉 Готовые примеры

### Минималистичный виджет

```tsx
<div className="bg-white rounded-xl shadow p-4">
  <div className="flex items-center gap-3">
    <span className="text-3xl">{weather.temp}°</span>
    <div>
      <p className="text-sm text-gray-600">{weather.city}</p>
      <p className="text-xs text-gray-400">{weather.description}</p>
    </div>
  </div>
</div>
```

### Компактный виджет

```tsx
<div className="flex items-center gap-2 bg-blue-500 text-white rounded-full px-4 py-2">
  <span className="text-lg font-bold">{weather.temp}°</span>
  <span className="text-sm opacity-80">{weather.city}</span>
</div>
```

---

**Лимиты бесплатного плана**: 1000 запросов/день, обновление каждые 10 минут  
**Рекомендуемый интервал**: 30-60 минут для экономии запросов  
**Кэширование**: Next.js автоматически кэширует на 30 минут (revalidate: 1800)

