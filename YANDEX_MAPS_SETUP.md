# 🗺️ Настройка Яндекс Карт

## Получение API ключа

### Шаг 1: Регистрация
1. Перейдите на [Яндекс Карты API](https://developer.tech.yandex.ru/)
2. Войдите в свой аккаунт Яндекс или создайте новый
3. Перейдите в раздел "JavaScript API и HTTP Геокодер"

### Шаг 2: Создание ключа
1. Нажмите "Получить ключ"
2. Заполните информацию о проекте:
   - **Название**: ThaiWay
   - **Описание**: Путеводитель по Таиланду
   - **Тип**: JavaScript API
3. Укажите домены (для разработки можно использовать `localhost`)
4. Согласитесь с условиями
5. Нажмите "Создать ключ"

### Шаг 3: Настройка проекта

#### Вариант А: Через .env файл (рекомендуется)

1. Создайте файл `.env.local` в корне проекта:

```bash
# Windows (Git Bash)
touch .env.local

# Windows (PowerShell)
New-Item .env.local
```

2. Добавьте ваш API ключ:

```env
NEXT_PUBLIC_YANDEX_MAPS_API_KEY=ваш_ключ_здесь
```

3. Перезапустите dev-сервер:

```bash
npm run dev
```

#### Вариант Б: Прямо в коде (НЕ рекомендуется для production)

Отредактируйте файл `src/shared/api/yandex-maps.ts`:

```typescript
export const YANDEX_MAPS_CONFIG = {
  apiKey: 'ваш_ключ_здесь', // Замените на ваш ключ
  version: '2.1',
  lang: 'ru_RU',
} as const;
```

⚠️ **Внимание**: Не коммитьте файлы с реальными API ключами в Git!

## Проверка работы

1. Откройте [http://localhost:3000](http://localhost:3000)
2. На главной странице должна отобразиться карта Таиланда
3. Если карта не загружается:
   - Проверьте консоль браузера (F12)
   - Убедитесь что API ключ установлен правильно
   - Проверьте что домен разрешен в настройках ключа

## Настройка карты

### Изменение центра и зума

Отредактируйте `src/shared/api/yandex-maps.ts`:

```typescript
// Координаты центра (широта, долгота)
export const THAILAND_CENTER = {
  lat: 13.736717,  // Широта Бангкока
  lng: 100.523186, // Долгота Бангкока
} as const;

// Уровень зума (чем больше - тем ближе)
// 1 - весь мир
// 6 - страна
// 10 - город
// 15 - район
// 18 - улица
export const THAILAND_ZOOM = 6;
```

### Изменение размера карты

В `src/app/page.tsx`:

```tsx
<YandexMap 
  width={1000}   // Ширина в пикселях
  height={1000}  // Высота в пикселях
/>
```

### Популярные координаты Таиланда

```typescript
// Бангкок
{ lat: 13.736717, lng: 100.523186 }

// Пхукет
{ lat: 7.880758, lng: 98.392179 }

// Чиангмай
{ lat: 18.788135, lng: 98.985520 }

// Паттайя
{ lat: 12.927608, lng: 100.877083 }

// Краби
{ lat: 8.086447, lng: 98.906311 }
```

## Дополнительные возможности

### Добавление маркера

```typescript
// В компоненте YandexMap
const placemark = new window.ymaps.Placemark(
  [13.736717, 100.523186],
  {
    balloonContent: 'Бангкок'
  },
  {
    preset: 'islands#redIcon'
  }
);

mapInstanceRef.current.geoObjects.add(placemark);
```

### Добавление элементов управления

В `src/widgets/yandex-map/ui/yandex-map.tsx`:

```typescript
mapInstanceRef.current = new window.ymaps.Map(mapRef.current, {
  center: [THAILAND_CENTER.lat, THAILAND_CENTER.lng],
  zoom: THAILAND_ZOOM,
  controls: [
    'zoomControl',        // Кнопки зума
    'fullscreenControl',  // Полноэкранный режим
    'typeSelector',       // Выбор типа карты
    'geolocationControl', // Геолокация
    'searchControl',      // Поиск
    'routeButtonControl', // Маршруты
    'trafficControl',     // Пробки
  ],
});
```

## Troubleshooting

### Карта не загружается
- ✅ Проверьте API ключ
- ✅ Проверьте консоль браузера на ошибки
- ✅ Убедитесь что домен разрешен в настройках ключа Яндекс

### Карта серая
- ✅ Проверьте интернет соединение
- ✅ Проверьте что координаты корректны
- ✅ Проверьте лимиты API ключа

### Карта не центрирована на Таиланде
- ✅ Проверьте координаты в `src/shared/api/yandex-maps.ts`
- ✅ Попробуйте изменить уровень зума

## Полезные ссылки

- [Документация Яндекс.Карт](https://yandex.ru/dev/maps/jsapi/doc/2.1/)
- [Примеры использования](https://yandex.ru/dev/maps/jsapi/doc/2.1/examples/)
- [API Console](https://developer.tech.yandex.ru/)

---

**Примечание**: Для production использования необходимо получить коммерческий API ключ или проверить лимиты бесплатного использования.

