# 🗺️ Границы Таиланда на карте

## Обзор

Теперь на карте отображаются границы Таиланда в виде полигона. Это помогает визуально выделить страну и сделать карту более информативной.

## ✨ Возможности

- ✅ Отображение границ Таиланда
- ✅ Настраиваемые цвета и стили
- ✅ Полупрозрачная заливка
- ✅ Легко включается/выключается
- ✅ Не мешает работе с маркерами

## 🎨 Использование

### Базовое использование

```tsx
<YandexMap 
  width={1000} 
  height={1000}
  showBorders={true}  // Включить отображение границ
/>
```

### С кастомным стилем

```tsx
import { BORDER_STYLE_BLUE } from '@/shared/data';

<YandexMap 
  showBorders={true}
  borderStyle={BORDER_STYLE_BLUE}
/>
```

### Свой стиль

```tsx
<YandexMap 
  showBorders={true}
  borderStyle={{
    strokeColor: '#00FF00',    // Зеленая линия
    strokeOpacity: 1,          // Непрозрачная
    strokeWidth: 4,            // Толщина 4px
    fillColor: '#00FF00',      // Зеленая заливка
    fillOpacity: 0.2,          // 20% прозрачности
  }}
/>
```

## 🎨 Готовые стили

### 1. Красный (по умолчанию)
```typescript
import { BORDER_STYLE } from '@/shared/data';

borderStyle={BORDER_STYLE}
```
- Цвет: Красный (#FF6B6B)
- Заливка: 10% прозрачности
- Линия: 3px

### 2. Синий
```typescript
import { BORDER_STYLE_BLUE } from '@/shared/data';

borderStyle={BORDER_STYLE_BLUE}
```
- Цвет: Синий (#1E98FF)
- Заливка: 5% прозрачности
- Линия: 3px

### 3. Минималистичный
```typescript
import { BORDER_STYLE_MINIMAL } from '@/shared/data';

borderStyle={BORDER_STYLE_MINIMAL}
```
- Цвет: Темно-серый (#2C3E50)
- Заливка: Отсутствует
- Линия: 2px

## ⚙️ Настройки стиля

### Параметры

| Параметр | Тип | Описание | Пример |
|----------|-----|----------|--------|
| `strokeColor` | string | Цвет линии границы | `'#FF6B6B'` |
| `strokeOpacity` | number | Прозрачность линии (0-1) | `0.8` |
| `strokeWidth` | number | Толщина линии в пикселях | `3` |
| `fillColor` | string | Цвет заливки | `'#FF6B6B'` |
| `fillOpacity` | number | Прозрачность заливки (0-1) | `0.1` |

### Примеры цветов

```typescript
// Красный
strokeColor: '#FF6B6B'

// Синий
strokeColor: '#1E98FF'

// Зеленый
strokeColor: '#4CAF50'

// Оранжевый
strokeColor: '#FF9800'

// Фиолетовый
strokeColor: '#9C27B0'

// Черный
strokeColor: '#000000'

// Прозрачный (только линия)
fillColor: 'transparent'
fillOpacity: 0
```

## 📝 Координаты границ

Координаты хранятся в файле `src/shared/data/thailand-borders.ts`.

### Текущая реализация

- ✅ **Упрощенная версия**: ~70 точек
- ✅ Покрывает основную форму страны
- ✅ Включает основные регионы
- ✅ Оптимизирована для производительности

### Улучшение точности

Для более детальной границы можно:

1. **Использовать GeoJSON**
   - Скачать с [Natural Earth](https://www.naturalearthdata.com/)
   - Или с [OpenStreetMap](https://www.openstreetmap.org/)

2. **Конвертировать в нужный формат**
   ```typescript
   // Пример структуры
   export const DETAILED_BORDER = [
     [lat1, lng1],
     [lat2, lng2],
     // ... сотни точек
   ];
   ```

3. **Заменить координаты**
   Обновить `THAILAND_BORDER_COORDINATES` в файле `thailand-borders.ts`

## 🔧 API компонента

### YandexMap Props

```typescript
interface YandexMapProps {
  width?: number;              // Ширина карты
  height?: number;             // Высота карты
  places?: Place[];            // Массив мест
  onPlaceClick?: (place: Place) => void;  // Обработчик клика
  showBorders?: boolean;       // Показать границы
  borderStyle?: BorderStyle;   // Стиль границ
}
```

### BorderStyle Type

```typescript
interface BorderStyle {
  strokeColor: string;         // Цвет линии
  strokeOpacity: number;       // Прозрачность линии (0-1)
  strokeWidth: number;         // Толщина линии (px)
  fillColor: string;           // Цвет заливки
  fillOpacity: number;         // Прозрачность заливки (0-1)
}
```

## 💡 Практические примеры

### Пример 1: Только границы (без заливки)

```tsx
<YandexMap 
  showBorders={true}
  borderStyle={{
    strokeColor: '#2C3E50',
    strokeOpacity: 0.8,
    strokeWidth: 3,
    fillColor: 'transparent',
    fillOpacity: 0,
  }}
/>
```

### Пример 2: Яркая заливка

```tsx
<YandexMap 
  showBorders={true}
  borderStyle={{
    strokeColor: '#4CAF50',
    strokeOpacity: 1,
    strokeWidth: 2,
    fillColor: '#4CAF50',
    fillOpacity: 0.3,  // 30% прозрачности
  }}
/>
```

### Пример 3: Пунктирная линия (будущее)

Яндекс.Карты поддерживают пунктирные линии через `strokeStyle`:

```typescript
// В будущей версии можно добавить:
strokeStyle: 'shortdash'  // Короткий пунктир
strokeStyle: 'dash'       // Обычный пунктир
strokeStyle: 'dot'        // Точки
```

## 🎯 Советы

### Производительность

1. **Упрощенные координаты** достаточны для большинства случаев
2. **Кэширование**: Polygon создается один раз
3. **Ленивая загрузка**: Координаты импортируются динамически

### Визуальное сочетание

- **Тонкая линия + легкая заливка**: элегантно и не отвлекает
- **Яркая линия без заливки**: четкие границы
- **Контрастные цвета**: выделяет регион

### Доступность

- Границы не мешают взаимодействию с маркерами
- Hint "Таиланд" появляется при наведении
- Можно кликать на маркеры поверх границы

## 🐛 Troubleshooting

### Границы не отображаются

```tsx
// ❌ Неправильно
<YandexMap />

// ✅ Правильно
<YandexMap showBorders={true} />
```

### Неправильный цвет

Проверьте формат цвета:
```typescript
// ✅ Правильно
strokeColor: '#FF6B6B'
strokeColor: '#f00'
strokeColor: 'red'

// ❌ Неправильно
strokeColor: 'FF6B6B'  // Нет #
```

### Границы слишком яркие

Уменьшите opacity:
```typescript
fillOpacity: 0.05  // Очень легкая заливка
strokeOpacity: 0.5 // Полупрозрачная линия
```

## 🚀 Будущие улучшения

### Возможные дополнения:

1. **Несколько стран**
   - Показать соседние страны
   - Разные цвета для разных стран

2. **Регионы Таиланда**
   - Север, Северо-восток, Центр, Юг
   - Клик по региону → фильтр мест

3. **Интерактивность**
   - Клик по границе → информация о стране
   - Подсветка при наведении

4. **Анимация**
   - Плавное появление границ
   - Пульсация границы

## 📚 Ссылки

- [Яндекс.Карты Polygon API](https://yandex.ru/dev/maps/jsapi/doc/2.1/ref/reference/Polygon.html)
- [Natural Earth Data](https://www.naturalearthdata.com/) - точные границы стран
- [GeoJSON.io](http://geojson.io/) - редактор GeoJSON

---

**Примечание**: Текущие координаты границ являются упрощенными и подходят для визуализации. Для точных картографических приложений рекомендуется использовать официальные GeoJSON данные.

