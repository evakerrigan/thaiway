# ThaiWay - Путеводитель по Таиланду

Проект построен на архитектуре **Feature-Sliced Design (FSD)**.

## 📁 Структура проекта

```
src/
├── app/                    # Next.js App Router
│   ├── layout.tsx         # Корневой layout с Header и Footer
│   ├── page.tsx           # Главная страница (Страница 1) с картой
│   ├── page-2/            # Страница 2
│   │   └── page.tsx
│   └── page-3/            # Страница 3
│       └── page.tsx
├── widgets/               # Сложные UI-блоки
│   ├── header/           # Хедер с навигацией
│   │   ├── ui/
│   │   │   └── header.tsx
│   │   └── index.ts
│   ├── footer/           # Футер
│   │   ├── ui/
│   │   │   └── footer.tsx
│   │   └── index.ts
│   └── yandex-map/       # Виджет Яндекс карты
│       ├── ui/
│       │   └── yandex-map.tsx
│       └── index.ts
└── shared/               # Общие ресурсы
    ├── ui/              # UI-компоненты
    │   └── container/   # Контейнер для контента
    ├── config/          # Конфигурация
    │   └── routes.ts    # Роуты и навигация
    └── api/             # API конфигурация
        └── yandex-maps.ts  # Настройки Яндекс карт
```

## 🚀 Быстрый старт

### 1. Установка зависимостей

```bash
npm install
```

### 2. Настройка Яндекс Карт

Для работы карт необходим API ключ от Яндекс:

1. Зарегистрируйтесь на [Яндекс API](https://developer.tech.yandex.ru/)
2. Получите API ключ для JavaScript API Карт
3. Создайте файл `.env.local` в корне проекта:

```env
NEXT_PUBLIC_YANDEX_MAPS_API_KEY=ваш_api_ключ_здесь
```

### 3. Запуск проекта

```bash
npm run dev
```

Откройте [http://localhost:3000](http://localhost:3000) в браузере.

## 📄 Страницы

- **Страница 1** (`/`) - Главная страница с картой Таиланда
- **Страница 2** (`/page-2`) - Вторая страница
- **Страница 3** (`/page-3`) - Третья страница

## 🏗️ Архитектура FSD

Проект следует принципам Feature-Sliced Design:

### Слои (Layers)

1. **app** - инициализация приложения, провайдеры, роутинг
2. **widgets** - композитные блоки (Header, Footer, YandexMap)
3. **shared** - переиспользуемые модули (UI-kit, API, config)

### Преимущества архитектуры

- ✅ Четкое разделение ответственности
- ✅ Легкая масштабируемость
- ✅ Простота навигации по коду
- ✅ Упрощенное тестирование

## 🎨 Технологии

- **Next.js 16** - React фреймворк
- **TypeScript** - типизация
- **Tailwind CSS 4** - стилизация
- **Яндекс Карты API** - интерактивная карта

## 📝 Конфигурация

### Роуты (`shared/config/routes.ts`)

Централизованное управление маршрутами приложения.

### API (`shared/api/yandex-maps.ts`)

Конфигурация для Яндекс карт:
- Координаты центра Таиланда: `13.736717, 100.523186`
- Зум уровень: `6` (для отображения всей страны)

## 🔧 Разработка

```bash
# Запуск dev-сервера
npm run dev

# Сборка для продакшена
npm run build

# Запуск продакшен-сборки
npm run start

# Линтинг
npm run lint
```

## 📦 Добавление новых страниц

1. Создайте папку в `src/app/` с названием маршрута
2. Добавьте файл `page.tsx` внутри папки
3. Обновите `src/shared/config/routes.ts` для навигации

Пример:

```tsx
// src/app/new-page/page.tsx
import { Container } from '@/shared/ui/container';

export default function NewPage() {
  return (
    <Container className="py-12">
      <h1>Новая страница</h1>
    </Container>
  );
}
```

## 🗺️ Настройка карты

Параметры карты можно изменить в `src/shared/api/yandex-maps.ts`:

```typescript
export const THAILAND_CENTER = {
  lat: 13.736717,  // Широта
  lng: 100.523186, // Долгота
};

export const THAILAND_ZOOM = 6; // Уровень зума
```

---

Создано с ❤️ для путешествий по Таиланду

